name: flame_texturepacker
description: A simple plugin for Flame Engine to import spritesheets generated by the TexturePacker tool.
version: 2.1.0
homepage: https://github.com/Brixto/flame_texturepacker

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
